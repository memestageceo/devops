it breaks the contract that operators expect: status should be owned by the component that reconciles the object, and specs should be owned by the user. By patching through the main resource, you blur that line – you might accidentally overwrite a status field that the operator just set, or the operator may overwrite your manual change later because it sees a different resourceVersion.
