[project]
name = "operator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "kopf>=1.44.6",
    "kubernetes>=36.0.2",
]
